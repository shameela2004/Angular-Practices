import { ButtonEffectDirective } from './button-effect.directive';

describe('ButtonEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonEffectDirective();
    expect(directive).toBeTruthy();
  });
});
