import { NewdirDirective } from './newdir.directive';

describe('NewdirDirective', () => {
  it('should create an instance', () => {
    const directive = new NewdirDirective();
    expect(directive).toBeTruthy();
  });
});
